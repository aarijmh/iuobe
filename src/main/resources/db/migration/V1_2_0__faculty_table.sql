CREATE TABLE IF NOT EXISTS  `obe`.`faculty` (
                                 `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
                                 `name` VARCHAR(45) NOT NULL,
                                 `description` VARCHAR(145) NULL,
                                 PRIMARY KEY (`id`));